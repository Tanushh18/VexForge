{
  "name": "vexforge-hq",
  "version": "1.0.0",
  "private": true,
  "description": "VexForge HQ — internal AI company management console (org dashboard, CRM, lead pipeline, outreach queue, call support, voice chatbot)",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client && npm install --prefix worker",
    "dev": "concurrently -n server,client -c green,cyan \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "worker": "npm start --prefix worker",
    "worker:once": "npm run once --prefix worker",
    "seed": "npm run seed --prefix server",
    "test": "npm test --prefix server && npm test --prefix worker"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
